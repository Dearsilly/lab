# Model Configuration
model:
  name: "bert-base-uncased"
  max_length: 512
  num_labels: 1

training:
  learning_rate: 2.0e-5
  batch_size: 16
  num_epochs: 5
  early_stopping_patience: 3
  fp16: true
  weight_decay: 0.01
